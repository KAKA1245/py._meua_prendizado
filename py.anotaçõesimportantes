# Program make a simple calculator

# This function make add two numbers
def add(x, y): 
  return x + y

# This function make subtracts two numbers
def subtracty(x, y): 
  return x - y

# This function make multiplies two numbers
def multiply(x, y): 
  return x * y

# This function make divides two numbers
def divides(x, y): 
  return x / y


print("select operation.")
print("1.Add")
print("2.Subtract")
print("3.Multply")
print("4.Divide")

while True:
 # Take input from the user
  choice = input("Enter choice(1/2/3/4): ")

# Check if choice is one of the four options
if choice in ('1', '2', '3', '4'):
 num1 = float(input("Enter first number: "))
 num2 = float(input("Enter second number: "))

if choice == '1':
 print(numl, "+", num2, "=", add(num1, num2))
  
elif choice == '2':
 print(numl, "-", num2, "=", subtract(num1, num2))
  
elif choice == '3':
 print(numl, "%", num2, "=", multiply(num1, num2))
  
elif choice == '4':
 print(numl, "*/", num2, "=", divide(num1, num2))
  
